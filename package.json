{
  "name": "air-firebase",
  "version": "0.3.0",
  "description": "Provide a base class that implements functions for various firebase services and CRUD functions to Firestore.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "test": "jest",
    "test-local": "cross-env NODE_ENV=local node index.js",
    "test-dev": "cross-env NODE_ENV=dev node index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/shisyamo4131/air-firebase.git"
  },
  "author": "shisyamo4131",
  "license": "MIT",
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "firebase": "^10.6.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "jest": "^26.0.0"
  }
}
